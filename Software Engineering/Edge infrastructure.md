Talking about reverse proxy, API gateway and load balancer. Also talk about how and where auth and rate limiting is used in these.

Nginx acts as a reverse proxy and a load balancer.

An API gateway is also a specialized reverse proxy,
